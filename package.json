{
  "name": "@yusr_systems/yusr-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}